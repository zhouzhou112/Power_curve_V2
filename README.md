# Power_curve_V2

Code-only review repository for the 2020-2024 China provincial hourly load
calibration and decomposition pipeline.

Raw data and generated outputs are intentionally excluded. See
`PROJECT_STATUS_FOR_CHATGPT.md` for the current implementation status, review
priorities, and run command. See `codex_power_curve_v2_reset_plan.md` for the
full execution plan.

Local run command:

```powershell
& "C:\Users\ZZ\.conda\envs\RL\python.exe" -X utf8 scripts\run_pipeline.py
```
